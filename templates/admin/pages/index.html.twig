{% extends 'admin/base.html.twig' %}

{% block title %}Gestion des pages
{% endblock %}

{% block body %}
    <!-- Content Wrapper. Contains page content -->
    <div class="content-wrapper">
        {% include "admin/partials/sous-header.html.twig" with {'page': 'Pages'} %}

        <!-- Main content -->
        <section
            class="content">
            <!-- /.row -->
            <div class="row">
                <div class="col-xs-12">
                    <div class="box">
                        <div class="box-header box-tabl">
                            <h3 class="box-title">Gestion des pages</h3>

                            <div class="box-tools">
                                <div class="input-group input-group-sm" style="width: 150px;">
                                    <a href="{{path('admin_pages_ajout')}}" class="btn btn-primary">
                                        <i class="fa fa-plus"></i>
                                        Ajouter une page</a>
                                </div>
                            </div>
                        </div>
                        <!-- /.box-header -->
                        <div class="box-body table-responsive no-padding">
                            <table class="table table-hover">
                                <tr>
                                    <th>ID</th>
                                    <th>Titre</th>
                                    <th>Date création</th>
                                    <th>Menu</th>
                                    <th>Ordre menu</th>
                                    <th>Status</th>
                                    <th></th>
                                </tr>
                                {% for page in pages %}
                                    <tr>
                                        <td>{{page.id}}</td>
                                        <td>{{page.titre}}</td>
                                        <td>{{page.dateCreation|date('d-m-Y H:i')}}</td>
                                        <td>{{page.menu}}</td>
                                        <td>
                                            <span class="badge badge-dark">{{page.ordre}}</span>
                                        </td>
                                        <td>
                                            {% if page.publication == 1 %}
                                                <span class="label label-success">Publié</span>
                                            {% else %}
                                                <span class="label label-warning">En attente</span>
                                            {% endif %}
                                        </td>
                                        <td>
                                            <a href="{{path('admin_pages_edit',{'id':page.id})}}" class="btn btn-info" title="modifier">
                                                <i class="fa fa-edit"></i>
                                            </a>
                                            <a href="{{path('admin_pages_delete',{'id':page.id})}}" class="btn btn-danger" title="Supprimer">
                                                <i class="fa fa-trash"></i>
                                            </a>
                                        </td>
                                    </tr>
                                {% endfor %}
                            </table>
                        </div>
                        <!-- /.box-body -->
                    </div>
                    <!-- /.box -->
                </div>
            </div>
        </section>
        <!-- /.content -->
    </div>
{% endblock %}
